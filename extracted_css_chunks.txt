--- CHUNK 0 ---
  30% {
    opacity: 0.85;
    transform: scale(0.96) translateY(-4px);
    filter: blur(0.5px);
  }
  100% {
    opacity: 0;
    transform: scale(0.72) translateY(22px);
    filter: blur(8px);
  }
}

/* ==========================================================================
   Splash Screen
   ========================================================================== */
.splash-screen-container {
  position: fixed;
  inset: 0;
  z-index: 999999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(10, 12, 18, 0.8);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}

.splash-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  animation: splashFadeIn 0.3s ease forwards;
}

.splash-brand {
  font-family: 'Chelsea Market', cursive, system-ui !important;
  font-size: 42px;
  color: #fff;
  letter-spacing: 1px;
}

.splash-spinner {
  color: #ffbd2e;
  animation: spin 1s linear infinite;
}

@keyframes splashFadeIn {
  0% {
    opacity: 0;
    transform: scale(0.95);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes spin {
  100% {
    transform: rotate(360deg);
  }
}

/* ==========================================================================
   Rounding Overrides
   ========================================================================== */
[data-rounding="rectangle"] .arc-command-bar,
[data-rounding="rectangle"] .preset-chip,
[data-rounding="rectangle"] .segmented-btn,
[data-rounding="rectangle"] .saved-rig-card,
[data-rounding="rectangle"] .brand-pill,
[data-rounding="rectangle"] .game-card,
[data-rounding="rectangle"] .spec-select,
[data-rounding="rectangle"] .rt-info-tooltip,
[data-rounding="rectangle"] .pt-info-tooltip,
[data-rounding="rectangle"] .btn-primary,
[data-rounding="rectangle"] .btn-secondary,
[data-rounding="rectangle"] .btn-save-template,
[data-rounding="rectangle"] .btn-save-confirm,
[data-rounding="rectangle"] .btn-save-cancel,
[data-rounding="rectangle"] .save-rig-input,
[data-rounding="rectangle"] .arc-settings-btn,
[data-rounding="rectangle"] .popout-seg-btn,
[data-rounding="rectangle"] .switch-container,
[data-rounding="rectangle"] .system-status-bar,
[data-rounding="rectangle"] .search-input-wrap,
[data-rounding="rectangle"] .search-input {
  border-radius: 8px !important;
}

[data-rounding="rectangle"] .switch-slider::before {
  border-radius: 6px !important;
}

[data-rounding="rectangle"] .game-card-hover-actions {
  border-radius: 8px !important;
}

[data-rounding="rectangle"] .tab-btn,
[data-rounding="rectangle"] .game-tag,
[data-rounding="rectangle"] .spec-badge {
  border-radius: 6px !important;
}

[data-rounding="rectangle"] .component-square-box {
  border-radius: 8px !important;
}


--- CHUNK 1 ---
.specs-sidebar::-webkit-scrollbar-track {
  background: transparent;
}

--- CHUNK 2 ---
.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 0 16px 0;
  border-bottom: 1px solid var(--ctp-surface1);

--- CHUNK 3 ---
.spec-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 4px 0;
}

--- CHUNK 4 ---
.arc-sidebar .profile-popout {
  position: absolute;
  top: -8px;
  left: calc(100% + 14px);
  right: auto;
  width: 400px;
  background: rgba(32, 34, 42, 0.75);
  backdrop-filter: blur(28px) saturate(1.5) brightness(1.3);
  -webkit-backdrop-filter: blur(28px) saturate(1.5) brightness(1.3);

--- CHUNK 5 ---
.profile-popout {
  position: absolute;
  top: calc(100% + 12px);
  right: 0;
  width: 380px;
  background: rgba(32, 34, 42, 0.75);
  backdrop-filter: blur(28px) saturate(1.8) brightness(1.3);
  -webkit-backdrop-filter: blur(28px) saturate(1.8) brightness(1.3);

--- CHUNK 6 ---
.spec-select,
.sort-select {
  width: 100%;
  background-color: rgba(11, 13, 20, 0.5);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  color: #f1f5f9;

--- CHUNK 7 ---
[data-rounding="rectangle"] .component-square-box {
  border-radius: 8px !important;
}

/* ==========================================================================
   Splash Screen Transitions & Material Spinner
   ========================================================================== */
.splash-screen-container {
  transition: opacity 0.3s ease, filter 0.3s ease;
}

.splash-fade-out {
  opacity: 0;
  filter: blur(4px);
  pointer-events: none;
}

.material-spinner {
  animation: mat-rotate 2s linear infinite;
  width: 28px;
  height: 28px;
}

.material-spinner .path {
  stroke: #ffffff;
  stroke-linecap: round;
  animation: mat-dash 1.5s ease-in-out infinite;
}

@keyframes mat-rotate {
  100% {
    transform: rotate(360deg);
  }
}

@keyframes mat-dash {
  0% {
    stroke-dasharray: 1, 150;
    stroke-dashoffset: 0;
  }
  50% {
    stroke-dasharray: 90, 150;
    stroke-dashoffset: -35;
  }
  100% {
    stroke-dasharray: 90, 150;
    stroke-dashoffset: -124;
  }
}

/* ==========================================================================
   Global Ripple Effect
   ========================================================================== */
.global-ripple {
  position: absolute;
  border-radius: 50%;
  transform: scale(0);
  animation: ripple-anim 600ms linear;
  background-color: rgba(255, 255, 255, 0.2);
  pointer-events: none;
  z-index: 10;
}

@keyframes ripple-anim {
  to {
    transform: scale(4);
    opacity: 0;
  }
}

--- CHUNK 8 ---
.spec-select,
.sort-select {
  width: 100%;
  background-color: rgba(11, 13, 20, 0.25);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

--- CHUNK 9 ---
.switch-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(18, 18, 28, 0.25);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

--- CHUNK 10 ---
.switch-label:hover {
  background: rgba(26, 26, 38, 0.4);
  border-color: rgba(255, 255, 255, 0.22);
}

--- CHUNK 11 ---
.splash-screen-container {
  position: fixed;
  inset: 0;
  z-index: 999999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #04050a;
}

--- CHUNK 12 ---
.arc-sidebar {
  width: 320px;
  min-width: 320px;
  max-width: 320px;
  position: sticky;
  top: 0;
  left: 0;
  height: 100vh;
  max-height: 100vh;
  border-right: 1px solid rgba(255, 255, 255, 0.12);
  z-index: 100;
}

.arc-sidebar::before {
  content: '';
  position: absolute;
  inset: 0;
  z-index: -1;
  background: rgba(14, 18, 30, 0.48);
  backdrop-filter: blur(28px) saturate(1.7) brightness(1.06);
  -webkit-backdrop-filter: blur(28px) saturate(1.7) brightness(1.06);
}

--- CHUNK 13 ---
.carousel-slide-anim-next {
  animation: carousel-slide-next 0.7s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.carousel-slide-anim-prev {
  animation: carousel-slide-prev 0.7s cubic-bezier(0.16, 1, 0.3, 1) both;
}

--- CHUNK 14 ---
.spec-select,
.sort-select {
  width: 100%;
  background-color: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

--- CHUNK 15 ---
.switch-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

--- CHUNK 16 ---
.switch-label:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: rgba(255, 255, 255, 0.22);

--- CHUNK 17 ---
.modal-inner {
  position: relative;
  background: rgba(14, 16, 22, 0.65);
  backdrop-filter: blur(28px) saturate(1.4);
  -webkit-backdrop-filter: blur(28px) saturate(1.4);
  width: 100%;
  max-width: 580px;
  border-radius: var(--radius-lg);
  border: 1px solid rgba(255, 255, 255, 0.12);

--- CHUNK 18 ---
[data-theme="catppuccin-mocha"] .arc-sidebar::before {
  background: rgba(30, 30, 46, 0.55) !important;
  backdrop-filter: blur(28px) saturate(1.5) brightness(1.05) !important;
  -webkit-backdrop-filter: blur(28px) saturate(1.5) brightness(1.05) !important;
}
[data-theme="catppuccin-mocha"] .arc-sidebar {
  border-color: rgba(203, 166, 247, 0.18) !important;
  border-radius: 0 !important;

--- CHUNK 19 ---
[data-rounding="rectangle"] .game-card,
[data-rounding="rectangle"] .carousel-container,
[data-rounding="rectangle"] .carousel-slide-backdrop,
[data-rounding="rectangle"] .spec-select,

--- CHUNK 20 ---
.arc-sidebar {
  width: 320px;
  min-width: 320px;
  max-width: 320px;
  position: sticky;
  top: 0;
  left: 0;
  height: 100vh;
  max-height: 100vh;
  border-right: 1px solid rgba(255, 255, 255, 0.12);
  z-index: 100;
}

.arc-sidebar::before {
  content: '';
  position: absolute;
  inset: 0;
  z-index: -1;
  background: rgba(14, 18, 30, 0.48);
  backdrop-filter: blur(28px) saturate(1.7) brightness(1.06);
  -webkit-backdrop-filter: blur(28px) saturate(1.7) brightness(1.06);
}

--- CHUNK 21 ---
.carousel-slide-anim-next {
  animation: carousel-slide-next 0.7s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.carousel-slide-anim-prev {
  animation: carousel-slide-prev 0.7s cubic-bezier(0.16, 1, 0.3, 1) both;
}

--- CHUNK 22 ---
.spec-select,
.sort-select {
  width: 100%;
  background-color: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

--- CHUNK 23 ---
.switch-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

--- CHUNK 24 ---
.switch-label:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: rgba(255, 255, 255, 0.22);

--- CHUNK 25 ---
.modal-inner {
  position: relative;
  background: rgba(14, 16, 22, 0.65);
  backdrop-filter: blur(28px) saturate(1.4);
  -webkit-backdrop-filter: blur(28px) saturate(1.4);
  width: 100%;
  max-width: 580px;
  border-radius: var(--radius-lg);
  border: 1px solid rgba(255, 255, 255, 0.12);

--- CHUNK 26 ---
[data-theme="catppuccin-mocha"] .arc-sidebar::before {
  background: rgba(30, 30, 46, 0.55) !important;
  backdrop-filter: blur(28px) saturate(1.5) brightness(1.05) !important;
  -webkit-backdrop-filter: blur(28px) saturate(1.5) brightness(1.05) !important;
}
[data-theme="catppuccin-mocha"] .arc-sidebar {
  border-color: rgba(203, 166, 247, 0.18) !important;
  border-radius: 0 !important;

--- CHUNK 27 ---
  }
}

/* ==========================================================================
   Rounding Overrides
   ========================================================================== */
[data-rounding="rectangle"] .arc-command-bar,
[data-rounding="rectangle"] .preset-chip,
[data-rounding="rectangle"] .segmented-btn,
[data-rounding="rectangle"] .saved-rig-card,
[data-rounding="rectangle"] .brand-pill,
[data-rounding="rectangle"] .game-card,
[data-rounding="rectangle"] .carousel-container,
[data-rounding="rectangle"] .carousel-slide-backdrop,
[data-rounding="rectangle"] .spec-select,
[data-rounding="rectangle"] .rt-info-tooltip,
[data-rounding="rectangle"] .pt-info-tooltip,
[data-rounding="rectangle"] .btn-primary,
[data-rounding="rectangle"] .btn-secondary,
[data-rounding="rectangle"] .btn-save-template,
[data-rounding="rectangle"] .btn-save-confirm,
[data-rounding="rectangle"] .btn-save-cancel,
[data-rounding="rectangle"] .save-rig-input,
[data-rounding="rectangle"] .arc-settings-btn,
[data-rounding="rectangle"] .popout-seg-btn,
[data-rounding="rectangle"] .switch-container,
[data-rounding="rectangle"] .system-status-bar,
[data-rounding="rectangle"] .search-input-wrap,
[data-rounding="rectangle"] .search-input {
  border-radius: 8px !important;
}

[data-rounding="rectangle"] .switch-slider::before {
  border-radius: 6px !important;
}

[data-rounding="rectangle"] .game-card-hover-actions {
  border-radius: 8px !important;
}

[data-rounding="rectangle"] .tab-btn,
[data-rounding="rectangle"] .game-tag,
[data-rounding="rectangle"] .spec-badge {
  border-radius: 6px !important;
}

[data-rounding="rectangle"] .component-square-box {
  border-radius: 8px !important;
}

/* ==========================================================================
   Splash Screen Transitions & Material Spinner
   ========================================================================== */
.splash-screen-container {
  position: fixed;
  inset: 0;
  z-index: 999999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #04050a;
  transition: opacity 0.3s ease, filter 0.3s ease;
}

.splash-fade-out {
  opacity: 0;
  filter: blur(4px);
  pointer-events: none;
}

.material-spinner {
  animation: mat-rotate 2s linear infinite;
  width: 28px;
  height: 28px;
}

.material-spinner .path {
  stroke: #ffffff;
  stroke-linecap: round;
  animation: mat-dash 1.5s ease-in-out infinite;
}

@keyframes mat-rotate {
  100% {
    transform: rotate(360deg);
  }
}

@keyframes mat-dash {
  0% {
    stroke-dasharray: 1, 150;
    stroke-dashoffset: 0;
  }
  50% {
    stroke-dasharray: 90, 150;
    stroke-dashoffset: -35;
  }
  100% {
    stroke-dasharray: 90, 150;
    stroke-dashoffset: -124;
  }
}

/* ==========================================================================
   Global Ripple Effect
   ========================================================================== */
.global-ripple {
  position: absolute;
  border-radius: 50%;
  transform: scale(0);
  animation: ripple-anim 600ms linear;
  background-color: rgba(255, 255, 255, 0.2);
  pointer-events: none;
  z-index: 10;
}

@keyframes ripple-anim {
  to {
    transform: scale(4);
    opacity: 0;
  }
}

--- CHUNK 28 ---
@keyframes card-delete-anim {
  0% {
    opacity: 1;
    transform: scale(1) translateY(0);
    filter: blur(0px);
  }
  30% {
    opacity: 0.85;
    transform: scale(0.96) translateY(-4px);
    filter: blur(0.5px);
  }
  100% {
    opacity: 0;
    transform: scale(0.72) translateY(22px);
    filter: blur(8px);
  }
}

/* ==========================================================================
   Rounding Overrides
   ========================================================================== */
[data-rounding="rectangle"] .arc-command-bar,
[data-rounding="rectangle"] .preset-chip,
[data-rounding="rectangle"] .segmented-btn,
[data-rounding="rectangle"] .saved-rig-card,
[data-rounding="rectangle"] .brand-pill,
[data-rounding="rectangle"] .game-card,
[data-rounding="rectangle"] .carousel-container,
[data-rounding="rectangle"] .carousel-slide-backdrop,
[data-rounding="rectangle"] .spec-select,
[data-rounding="rectangle"] .rt-info-tooltip,
[data-rounding="rectangle"] .pt-info-tooltip,
[data-rounding="rectangle"] .btn-primary,
[data-rounding="rectangle"] .btn-secondary,
[data-rounding="rectangle"] .btn-save-template,
[data-rounding="rectangle"] .btn-save-confirm,
[data-rounding="rectangle"] .btn-save-cancel,
[data-rounding="rectangle"] .save-rig-input,
[data-rounding="rectangle"] .arc-settings-btn,
[data-rounding="rectangle"] .popout-seg-btn,
[data-rounding="rectangle"] .switch-container,
[data-rounding="rectangle"] .system-status-bar,
[data-rounding="rectangle"] .search-input-wrap,
[data-rounding="rectangle"] .search-input {
  border-radius: 8px !important;
}

[data-rounding="rectangle"] .switch-slider::before {
  border-radius: 6px !important;
}

[data-rounding="rectangle"] .game-card-hover-actions {
  border-radius: 8px !important;
}

[data-rounding="rectangle"] .tab-btn,
[data-rounding="rectangle"] .game-tag,
[data-rounding="rectangle"] .spec-badge {
  border-radius: 6px !important;
}

[data-rounding="rectangle"] .component-square-box {
  border-radius: 8px !important;
}

/* ==========================================================================
   Splash Screen Transitions & Material Spinner
   ========================================================================== */
.splash-screen-container {
  position: fixed;
  inset: 0;
  z-index: 999999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #04050a;
  transition: opacity 0.3s ease, filter 0.3s ease;
}

.splash-fade-out {
  opacity: 0;
  filter: blur(4px);
  pointer-events: none;
}

.material-spinner {
  animation: mat-rotate 2s linear infinite;
  width: 28px;
  height: 28px;
}

.material-spinner .path {
  stroke: #ffffff;
  stroke-linecap: round;
  animation: mat-dash 1.5s ease-in-out infinite;
}

@keyframes mat-rotate {
  100% {
    transform: rotate(360deg);
  }
}

@keyframes mat-dash {
  0% {
    stroke-dasharray: 1, 150;
    stroke-dashoffset: 0;
  }
  50% {
    stroke-dasharray: 90, 150;
    stroke-dashoffset: -35;
  }
  100% {
    stroke-dasharray: 90, 150;
    stroke-dashoffset: -124;
  }
}

/* ==========================================================================
   Global Ripple Effect
   ========================================================================== */
.global-ripple {
  position: absolute;
  border-radius: 50%;
  transform: scale(0);
  animation: ripple-anim 600ms linear;
  background-color: rgba(255, 255, 255, 0.2);
  pointer-events: none;
  z-index: 10;
}

@keyframes ripple-anim {
  to {
    transform: scale(4);
    opacity: 0;
  }
}

--- CHUNK 29 ---
/* ==========================================================================
   Global Ripple Effect
   ========================================================================== */
.ripple-active {
  position: relative !important;
  overflow: hidden !important;
}

.ripple-active::after {
  content: '';
  position: absolute;
  border-radius: 50%;
  background-color: rgba(255, 255, 255, 0.2);
  pointer-events: none;
  z-index: 10;
  
  /* Use CSS variables set by JS */
  left: var(--ripple-x);
  top: var(--ripple-y);
  width: var(--ripple-size);
  height: var(--ripple-size);
  margin-left: calc(var(--ripple-size) / -2);
  margin-top: calc(var(--ripple-size) / -2);
  
  transform: scale(0);
  animation: ripple-anim 600ms linear;
}

@keyframes ripple-anim {
  to {
    transform: scale(1);
    opacity: 0;
  }
}

--- CHUNK 30 ---
.arc-sidebar {
  width: 320px;
  min-width: 320px;
  max-width: 320px;
  position: sticky;
  top: 0;
  left: 0;
  height: 100vh;
  max-height: 100vh;
  border-right: 1px solid rgba(255, 255, 255, 0.12);
  z-index: 100;
  border-top: none;
  border-bottom: none;
  border-left: none;
  border-radius: 0 !important;
  display: flex;
  flex-direction: column;
  overflow: visible;
  transition: width 0.25s cubic-bezier(0.16, 1, 0.3, 1),
    min-width 0.25s cubic-bezier(0.16, 1, 0.3, 1),
    max-width 0.25s cubic-bezier(0.16, 1, 0.3, 1),
    transform 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  box-shadow: 4px 0 32px rgba(0, 0, 0, 0.25), inset -1px 0 0 rgba(255, 255, 255, 0.04);
}

.arc-sidebar::before {
  content: '';
  position: absolute;
  inset: 0;
  z-index: -1;
  background: rgba(14, 18, 30, 0.48);
  backdrop-filter: blur(28px) saturate(1.7) brightness(1.06);
  -webkit-backdrop-filter: blur(28px) saturate(1.7) brightness(1.06);
}

--- CHUNK 31 ---


--- CHUNK 32 ---
.app-container,
.app-shell {
  position: relative;
  z-index: 1;
  width: 100%;
  max-width: 100%;
  padding: 0;
  box-sizing: border-box;
  display: flex;
  flex-direction: row;
  gap: 0;
  min-height: 100vh;
}

